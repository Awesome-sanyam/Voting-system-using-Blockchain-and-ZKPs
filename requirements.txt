# ==========================================
# BlockVote-India - Full Python Dependencies
# ==========================================
# This file contains the combined Python dependencies 
# for both the Django Backend and the FastAPI ML Service.
# These dependencies are compatible with both macOS and Windows.

# --- Backend Dependencies (Django Core & Blockchain Relayer) ---
Django>=5.0.0
djangorestframework>=3.14.0
channels>=4.0.0
daphne>=4.0.0
web3>=6.10.0
psycopg2-binary>=2.9.9  # Pre-compiled PostgreSQL adapter (works on macOS/Windows)
redis>=5.0.1
python-dotenv>=1.0.0

# --- ML Microservice Dependencies (Active Sentinel) ---
fastapi>=0.100.0
uvicorn>=0.23.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.25.0

# ==========================================
# Non-Python Dependencies (Reference)
# ==========================================
# Note: The following are required system tools and environments 
# that cannot be installed via pip.

# 1. Node.js Environment (for Blockchain module)
#    - Node.js (v18.0.0 or higher)
#    - npm (v9.0.0 or higher)
#    - Packages: hardhat, @openzeppelin/contracts, circomlib, snarkjs

# 2. Frontend Environment
#    - Django Server-Side Rendered (SSR) Application
#    - Tailwind CSS (via CDN) + Modern Dark Theme UI
#    - Native WebSockets (Django Channels)
#    - Client-side zk-SNARK proof generation (SnarkJS / WebAssembly)

# 3. Database Services
#    - PostgreSQL (v15.0 or higher)
#    - Redis (v7.0 or higher)
#    - Docker & Docker Compose (Recommended for running DB services easily)
